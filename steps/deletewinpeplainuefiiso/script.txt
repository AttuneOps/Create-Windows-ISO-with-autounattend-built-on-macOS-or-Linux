if (${{kickstartedBootLoaderIsUefi}}) {
    
    $ISO_BUILD="C:\attuneautomationworker"
    Set-Location "${ISO_BUILD}"
    
    $WINPE_PLAIN_UEFI_ISO="winpe_plain_uefi.iso"
    if (Test-Path "${WINPE_PLAIN_UEFI_ISO}" -PathType Leaf) {
        Remove-Item "${WINPE_PLAIN_UEFI_ISO}"
    }
    
} else {
    echo "Skipping for BIOS ISO creation."
}
