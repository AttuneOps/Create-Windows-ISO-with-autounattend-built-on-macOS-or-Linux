if {isWin10Bios} || {isWinServerBios}
then

    DIRECTORY="{automationWorkerBaseDirectory}"
    cd "${DIRECTORY}"
    
    ISO="winpe_win2019_plain_bios.iso"
    if [[ -f ${ISO} ]]
    then
        echo "${DIRECTORY|/${ISO} exists. Great."
    else
        echo "${DIRECTORY|/${ISO} does not exist. Please run blueprint \"Create WinPE Plain Win2019 BIOS ISO\" to create it."
        exit 1
    fi
    
else
    echo "Skipping for UEFI ISO creation."
fi