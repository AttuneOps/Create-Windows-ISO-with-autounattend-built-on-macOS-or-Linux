if {isWin10Bios} || {isWinServerBios}
then
    PATH="{automationWorkerBaseDirectory}/build-{newOsNode.fqn}"
    mkdir --parents "${PATH}"
    cd "${PATH}"
    cp --verbose "../winpe_win2019_plain_bios.iso" "winpe.iso"
    
else
    echo "Skipping for UEFI ISO creation."
fi