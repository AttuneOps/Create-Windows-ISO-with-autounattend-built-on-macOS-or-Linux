BOOT="$(echo "{biosOrUefi}" | tr "[:lower:]" "[:upper:]")"
if [ "$BOOT" != "BIOS" ] && [ "$BOOT" != "UEFI" ]
then
    echo "Please set BIOS or UEFI to BIOS or UEFI." >&2
    false
fi

echo "SUCCESS: Ok"
