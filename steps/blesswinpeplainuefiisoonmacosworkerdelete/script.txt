if uname | \
    grep -iq "darwin"
then
    echo "is macOS"
    
    cd "{automationWorkerBaseDirectory}/build-winpe-iso"
    
    bless --verbose --file winpe_staging/efi/boot/bootx64.efi
   
else
    echo "not macOS, skipping"

fi

