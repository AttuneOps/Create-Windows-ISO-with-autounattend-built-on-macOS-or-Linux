if (${{kickstartedBootLoaderIsUefi}}) {
    
    $WinPE_amd64_PATH="C:\attuneautomationworker\build-winpe-iso\WinPE_amd64"
    
    Set-Location "${WinPE_amd64_PATH}"
    
    $FWFILES_FOLDER="fwfiles"
    if ( -Not (Test-Path "${FWFILES_FOLDER}") ) {
      New-Item "${FWFILES_FOLDER}" -ItemType Directory
    }
    
    Copy-Item -Recurse -Path "C:\WinPE_amd64\fwfiles\*" `
        -Destination "${FWFILES_FOLDER}"
        
} else {
    echo "Skipping for BIOS ISO creation."
}