if hostnamectl | grep --quiet --ignore-case "centos" || \
    hostnamectl | grep --quiet --ignore-case "red hat" 
then
    echo "This is a CentOS or Red Hat clone."
    dnf -y update
else

    echo "Error: implementation needed for this flavor of Linux" >&2
    false
fi