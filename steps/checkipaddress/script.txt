if (-Not (ipconfig | `
    findstr "IPv4 Address" | `
    findstr "{newWindowsMachine.ip}")) {
    Write-Host "Wrong IP Address."
    exit 1
} else {
    Write-Host "IP address {newWindowsMachine.ip} is correct."
}