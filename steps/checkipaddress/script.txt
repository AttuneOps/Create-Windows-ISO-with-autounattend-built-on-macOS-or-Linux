if (-Not (ipconfig | `
    findstr "IPv4 Address" | `
    findstr "{newMachine.ip}")) {
    Write-Host "Wrong IP Address."
    exit 1
} else {
    Write-Host "IP address {newMachine.ip} is correct."
}