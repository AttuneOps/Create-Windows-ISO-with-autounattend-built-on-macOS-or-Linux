Deletes the following:
1. Build directory at `{linuxWorkerDirectory}/build-{newMachine.fqn}`.`
2. Drivers drop in directory at `{linuxWorkerDirectory}/drivers-{newMachine.fqn}`.
3. Kickstart ISO at `{linuxWorkerDirectory}/{isoFilename}`.
4. Drivers ISO at `{linuxWorkerDirectory}/drivers_{newMachine.fqn}.iso`.