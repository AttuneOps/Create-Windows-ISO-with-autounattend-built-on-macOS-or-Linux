Deletes the following:
1. Build directory at `{linuxWorkerDirectory}/build-{newWindowsMachine.fqn}`.`
2. Drivers drop in directory at `{linuxWorkerDirectory}/drivers-{newWindowsMachine.fqn}`.
3. Kickstart ISO at `{linuxWorkerDirectory}/{isoFilename}`.
4. Drivers ISO at `{linuxWorkerDirectory}/drivers_{newWindowsMachine.fqn}.iso`.