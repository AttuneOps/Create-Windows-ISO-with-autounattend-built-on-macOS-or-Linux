# The wildcard will allow removal of 
# - unattended_{newMachine.fqn}.iso
# - drivers_{newMachine.fqn}
# - build_{newMachine.fqn}

for FILE in {linuxWorkerDirectory}*{newMachine.fqn}*
do
    echo "Removing ${FILE}"
    rm -frv "${FILE}"
done
