# The wildcard will allow removal of 
# - {isoFilename}
# - drivers_{newWindowsMachine.fqn}
# - build_{newWindowsMachine.fqn}

for FILE in {linuxWorkerDirectory}/*{newWindowsMachine.fqn}*
do
    echo "Removing ${FILE}"
    rm -frv "${FILE}"
done
