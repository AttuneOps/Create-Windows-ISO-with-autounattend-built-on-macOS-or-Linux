Runs the `Windows PE add-on for the ADK for Windows Server 2022` `adkwinpesetup.exe` and installs the feature `OptionId.WindowsPreinstallationEnvironment`.

To see all options:
```
adkswinpeetup.exe /?
```

To see all available features:
```
.\adkwinpesetup.exe /list
```