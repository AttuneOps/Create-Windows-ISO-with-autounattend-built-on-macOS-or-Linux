if {kickstartedBootLoaderIsUefi}
then

    cd "{linuxWorkerDirectory}/build-{newWindowsMachine.fqn}"
    
    USER="{linuxWorkerUser.user}"
    GROUP="$(id -gn {linuxWorkerUser.user})"
    echo "User: $USER, Group: $GROUP"
    
    chown -R -v "$USER:$GROUP" "winpe_staging"

else
    echo "Skipping for BIOS boot."
fi

