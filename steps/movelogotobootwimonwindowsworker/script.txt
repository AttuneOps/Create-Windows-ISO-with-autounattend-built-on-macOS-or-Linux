$ISO_BUILD="{automationWorkerWindowsBaseDirectory}"
$BUILD_DIR="${ISO_BUILD}\build-{newOsNode.fqn}"
$DESTINATION_PATH=".\WinPE_BootImageDir\Windows\Attune"
$FILE = "attune_logo.bmp"
Set-Location "${BUILD_DIR}"

if (-not (Test-Path -Path ${DESTINATION_PATH})) {
    New-Item -Path ${DESTINATION_PATH} -ItemType Directory
}

Move-Item -Path ".\${FILE}" `
    -Destination "${DESTINATION_PATH}\${FILE}"