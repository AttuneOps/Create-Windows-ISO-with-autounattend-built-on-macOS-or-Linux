if ( ${{isWin10Bios}} -Or ${{isWinServerBios}} ) {

    $DIRECTORY="C:\attuneautomationworker"
    Set-Location "${DIRECTORY}"
    
    $ISO="winpe_plain_bios.iso"

    if (test-Path "${ISO}" -pathtype leaf) {
        Write-Host "${DIRECTORY}\${ISO} exists. Great."
    } else {
        Write-Host "${DIRECTORY}\${ISO} does not exist. Please run blueprint \"Create Windows PE (WinPE) Plain ISO on Windows Worker\" to create it."
        exit 1
    }
    
} else {
    Write-Host "Skipping for UEFI ISO creation."
}