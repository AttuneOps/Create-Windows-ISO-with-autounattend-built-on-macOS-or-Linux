if (-Not (HostName | `
    findstr /I "{newMachine.hostname}")) {
    Write-Host "Hostname $(HostName) should be {newMachine.hostname}."
    exit 1
} else {
    Write-Host "Hostname $(HostName) is correct."
}