if (-Not (HostName | `
    findstr /I "{newWindowsMachine.hostname}")) {
    Write-Host "Hostname $(HostName) should be {newWindowsMachine.hostname}."
    exit 1
} else {
    Write-Host "Hostname $(HostName) is correct."
}