$ISO_BUILD="C:\attuneautomationworker"
$BUILD_DIR="$ISO_BUILD\build-winpe-iso"

Set-Location $BUILD_DIR

$FOLDER = "winpe_staging"

Copy-Item -Recurse -Path `
    "${FOLDER}\winimg\boot\etfsboot.com" `
    -Destination "${FOLDER}\BOOT"