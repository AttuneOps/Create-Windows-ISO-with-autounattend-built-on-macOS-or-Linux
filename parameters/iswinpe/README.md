Set `true` only for WinPE ISO Procedures. Leave `false` for Single ISO and Dual ISO.

When true, FirstLogon runs `C:\post_install_setup.ps1` (installed OS). Drivers still use Unattend Drive Letter (`X` on WinPE).
