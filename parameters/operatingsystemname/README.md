Target Windows edition. Use `Windows 10` or `Windows 11` for workstation, or `Windows Server 2016`, `Windows Server 2019`, or `Windows Server 2022`.

Replaces Is Windows Server and Is Windows Workstation. WinPE is a separate install method (`isWinPE`).
